# 08 Credit And Debit Notes

Purpose:

- test note import types
- validate note handling in transaction review and return preparation

Files:

- `credit_note_sample.csv`
- `debit_note_sample.csv`

Recommended steps:

1. Upload `credit_note_sample.csv` as `Credit note`
2. Upload `debit_note_sample.csv` as `Debit note`
3. Review the transactions in `Reports`
4. Prepare GSTR-1 again and inspect note-related sections where applicable

Expected outcome:

- note batches process successfully
- transaction types are classified correctly
- note documents are visible in reports and downstream exports

