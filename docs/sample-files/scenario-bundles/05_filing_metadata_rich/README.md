# 05 Filing Metadata Rich

Purpose:

- test richer filing metadata capture
- reduce the filing metadata warning
- improve GSTR-1 workbook details

Files:

- `sales_filing_metadata_rich.csv`

Recommended steps:

1. Upload the file as `Sales`
2. Open `Reports` and inspect transaction metadata
3. Open `Returns`
4. Prepare GSTR-1
5. Export the GSTR-1 workbook

Expected outcome:

- HSN, UQC, quantity, service flag, supply category, and e-commerce GSTIN should be captured
- readiness warnings for missing filing metadata should be lower or absent

